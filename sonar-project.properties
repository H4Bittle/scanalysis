sonar.projectKey=h4bittle_scanalysis
sonar.organization=h4bittle

sonar.projectName=scanalysis
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclude certain files if necessary
sonar.exclusions=**/build-wrapper-dump.json

# Include additional languages if necessary
sonar.inclusions=**/*.js,**/*.ts,**/*.html,**/*.css

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=scanalysis
#sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
